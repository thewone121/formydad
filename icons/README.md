# Icons

Standalone SVG icons (navy `#0a1e33` stroke) for reuse in documents, email
signatures, or as `<img>` sources. The website itself inlines equivalent
icons in the HTML (using `currentColor`) for performance and theme support.
